# 🎤 AI 助手语音功能使用指南

## ✨ 新增功能

### 1️⃣ **语音输入（STT - Speech-to-Text）**
**位置**：输入框左侧的麦克风按钮 🎤

**使用方法**：
1. 点击麦克风按钮开始录音（按钮会变红并脉冲动画）
2. 对着麦克风说话（中文）
3. 说完后自动识别并填入输入框
4. 自动发送消息给 AI

**支持的浏览器**：
- ✅ Chrome / Edge（推荐）
- ✅ Safari（macOS / iOS）
- ⚠️ Firefox（部分支持）

**常见问题**：
- **"未检测到语音"** → 说话声音太小或麦克风未开启
- **"需要麦克风权限"** → 浏览器弹窗点击"允许"
- **按钮不显示** → 浏览器不支持 Web Speech API

---

### 2️⃣ **语音播报（TTS - Text-to-Speech）**
**位置**：鼠标悬停在 AI 回复消息上时，右侧出现喇叭按钮 🔊

**使用方法**：
1. 鼠标移到 AI 的回复消息气泡上
2. 点击右侧出现的喇叭按钮
3. AI 开始朗读该消息（中文女声）
4. 再次点击可停止播放

**播放指示**：
- 播放中：喇叭按钮变红，消息气泡右上角显示 🔊
- 点击其他消息的喇叭会自动停止当前播放

**支持的浏览器**：
- ✅ 所有现代浏览器（Chrome / Safari / Firefox / Edge）

---

## 🔧 技术实现

### 语音输入（STT）
```javascript
// 使用浏览器原生 Web Speech API
const recognition = new webkitSpeechRecognition();
recognition.lang = 'zh-CN';
recognition.continuous = false;
recognition.interimResults = false;

recognition.onresult = (e) => {
  const transcript = e.results[0][0].transcript;
  // 自动填入输入框并发送
};
```

**优势**：
- ✅ 完全免费（无需调用云端 API）
- ✅ 响应速度快（本地识别）
- ✅ 零配置（浏览器内置）

**限制**：
- ⚠️ 需要 HTTPS（部署到 Netlify 自动满足）
- ⚠️ 首次使用需授权麦克风权限

---

### 语音播报（TTS）
```javascript
// 使用浏览器原生 Speech Synthesis API
const utterance = new SpeechSynthesisUtterance(text);
utterance.lang = 'zh-CN';
utterance.rate = 1.0;  // 语速（0.1-10）
utterance.pitch = 1.0; // 音调（0-2）

window.speechSynthesis.speak(utterance);
```

**优势**：
- ✅ 完全免费
- ✅ 支持多语言（自动检测中文）
- ✅ 可调节语速、音调

**限制**：
- ⚠️ 中文声音质量取决于系统（Windows < macOS < iOS）
- ⚠️ 长文本可能被截断（浏览器限制）

---

## 🎨 UI/UX 设计亮点

### 视觉反馈
1. **语音输入**：
   - 待机：灰色麦克风
   - 录音中：红色 + 脉冲动画
   - 识别完成：自动恢复

2. **语音播报**：
   - 默认：隐藏（鼠标悬停显示）
   - 播放中：喇叭按钮变红 + 消息右上角 🔊 图标动画

### 交互优化
- ✅ 语音输入后自动发送（减少操作步骤）
- ✅ 点击同一朗读按钮可停止播放
- ✅ 切换消息时自动停止上一条播放
- ✅ 关闭助手面板时停止所有朗读

---

## 📱 移动端支持

### iOS Safari
- ✅ 语音输入完美支持
- ✅ TTS 使用 Siri 语音引擎（质量最佳）
- ⚠️ 首次需手动授权麦克风

### Android Chrome
- ✅ 语音输入完美支持
- ✅ TTS 使用 Google TTS 引擎
- ⚠️ 部分国产浏览器可能不支持

---

## 🔒 隐私说明

**语音输入（STT）**：
- 数据处理：取决于浏览器实现
  - Chrome：发送到 Google 服务器识别
  - Safari：优先本地识别（iOS 13+）
- 不会存储录音文件
- 仅在点击麦克风按钮时才启动

**语音播报（TTS）**：
- 完全本地合成，不发送任何数据到服务器
- 使用系统内置语音引擎

---

## 🚀 未来扩展方向

### 可能的改进
- [ ] 支持语音指令（"帮我搜索 X"、"打开模块 3"）
- [ ] 多语言切换（英文/日文等）
- [ ] 语速/音调调节滑块
- [ ] 连续对话模式（持续监听）
- [ ] 语音情感识别（调整 AI 回复风格）

### 高级功能（需后端支持）
- [ ] 接入专业 TTS API（Azure / ElevenLabs）获得更自然的声音
- [ ] 语音克隆（自定义 AI 助手声音）
- [ ] 实时流式识别（边说边显示）

---

## 🐛 故障排查

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 麦克风按钮不显示 | 浏览器不支持 | 换用 Chrome/Edge |
| 点击麦克风无反应 | 未授权权限 | 地址栏左侧点击 🔒 → 允许麦克风 |
| 识别结果不准确 | 环境噪音/口音 | 靠近麦克风清晰说话 |
| TTS 无声音 | 系统音量关闭 | 检查设备音量 |
| TTS 声音卡顿 | 浏览器性能问题 | 关闭其他标签页 |

---

## 📚 相关资源

**MDN 文档**：
- [Web Speech API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)
- [SpeechRecognition](https://developer.mozilla.org/en-US/docs/Web/API/SpeechRecognition)
- [SpeechSynthesis](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis)

**浏览器兼容性**：
- [Can I Use - Speech Recognition](https://caniuse.com/speech-recognition)
- [Can I Use - Speech Synthesis](https://caniuse.com/speech-synthesis)

---

**💡 提示**：语音功能是纯前端实现，无需修改 Serverless Function 或添加环境变量，推送到 GitHub 后 Netlify 自动生效！
